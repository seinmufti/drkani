# Snapshot — Dr. Kani

Mobile-first. Capture at **390 × 844** (under the 900px breakpoint).

## Pages / states

1. Introduction (`#introduction`)
2. Menu open (hamburger)
3. Results (`#results`) — sample before/after slider
4. Credentials (`#certificates`)
5. Contact (`#contact`) — fill dummy form fields; do not submit

Skip footer / legal.

## Export

- Canvas: `#ebebeb`
- Instagram **1080 × 1080**
- Mobile frames: **fill top and bottom**; canvas only on **left and right** (pillarbox). No empty bands above/below.
